# Kernel source fetched via `apt-get source` for the ipu-bridge rebuild.
# Regenerated by setup_ipu_bridge_mod.sh; far too large to track.
/kernel-src/
/linux-oem-*
linux-*.dsc
linux-*.diff.gz
linux-*.orig.tar.gz

# Build scratch
*.tmp
